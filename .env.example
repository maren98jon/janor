# Development (Docker PostgreSQL)
# Use this for local development with docker-compose
DATABASE_URL="postgresql://janor:janor_secret@localhost:5433/janor_dev?schema=public"

# Production (Supabase / Neon / Railway)
# Get this from your database provider's connection string page
# For Supabase: Settings → Database → Connection string → Transaction (port 6543)
# DATABASE_URL="postgresql://postgres.[project-id]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres"
